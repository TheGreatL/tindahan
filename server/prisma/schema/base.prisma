generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum StockLevel {
  IN_STOCK
  LOW_STOCK
  CRITICAL
  OVERSTOCK
  OUT_OF_STOCK
}

enum DebStatus {
  PAID
  UN_PAID
  PARTIALLY_PAID
}
